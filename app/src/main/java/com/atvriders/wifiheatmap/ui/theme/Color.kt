package com.atvriders.wifiheatmap.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette: emerald (strong-signal green) primary with an amber accent,
// echoing the fixed RSSI heatmap scale without competing with it.
val Emerald = Color(0xFF37C68F)
val EmeraldDark = Color(0xFF00754F)
val Amber = Color(0xFFFFC107)
val DeepPine = Color(0xFF0E1F1A)
