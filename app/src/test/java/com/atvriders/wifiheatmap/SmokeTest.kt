package com.atvriders.wifiheatmap

import org.junit.Assert.assertTrue
import org.junit.Test

/** Placeholder so testReleaseUnitTest is non-empty from the first CI run. */
class SmokeTest {
    @Test
    fun scaffoldIsAlive() {
        assertTrue(true)
    }
}
